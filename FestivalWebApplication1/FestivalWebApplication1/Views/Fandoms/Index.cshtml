@model IEnumerable<FestivalWebApplication1.Models.Fandom>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FandomName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FandomName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FandomId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FandomId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FandomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
