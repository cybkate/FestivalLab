@model IEnumerable<FestivalWebApplication1.Models.CosplayerTeam>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CosplayerTeamName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CosplayerTeamName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CosplayerTeamId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CosplayerTeamId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CosplayerTeamId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
